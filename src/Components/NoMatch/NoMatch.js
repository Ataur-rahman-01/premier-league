import React from "react";
import Banner from "../Banner/Banner";

const NoMatch = () => {
  return (
    <div>
      <Banner></Banner>
      <h2 style={{ textAlign: "center" }}>Route can not found</h2>
    </div>
  );
};

export default NoMatch;
